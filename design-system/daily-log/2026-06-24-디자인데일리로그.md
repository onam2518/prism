# Prism 디자인 데일리 로그 — 2026-06-24

> 프로젝트 데일리로그(`prism/daily-log/`)와 분리된 **디자인 전용 로그**. 디자인 시스템·위젯-홈·캐릭터·로고·가이드 작업만 기록.

Desktop의 `DESIGN.md`(Perplexity prose-derived 토큰)를 기준으로 **`@prism/design-system`을 Perplexity 정렬(따뜻한 페이퍼 + Peacock teal · 라이트 우선)으로 전면 구축**하고, **위젯-홈 중심 아키텍처**까지 확립. 위치 `~/Desktop/project/prism/design-system`.

## 한 일

### 1. 디자인 시스템 구축 (토큰 + 컴포넌트 50+)
- 기존 Upstage 다크(violet) → **Perplexity teal 라이트로 전면 교체**(v0.2.0). 단일 토큰 소스 `tokens.json`→`theme.css`(라이트 기본 + `.ds-dark` 1급 다크).
- **컴포넌트 50+**: Primitive(Button/IconButton/Input/Composer/Select/Card/Badge+메타칩/Tabs/Table/Dialog/Toast/Toggle/Steps/AttachmentChip) · Infographic(Stat/ProgressBar/ProgressRing/Skeleton/Spinner) · App UI(AppShell/Sidebar/StatusDot/Segmented/KeyField/Accordion/FileDropzone/DefinitionList/GradePill/EmptyState/CodeBlock) · Widget(Widget/WidgetGrid/LauncherWidget) · 안내(Coachmark/Assistant).
- 전부 토큰 기반·thin-wrapper(`ds-*` 클래스 + components.css). 44개 파일 트랜스파일 클린, CSS 변수 cross-check 통과.
- **브랜드 결정 확정**: 앱 전체를 teal 라이트로 이행(violet 폐기). serve.py 이행 매핑(`#0b0a0f→#fbfaf4` 등) 문서화.

### 2. 위젯-홈 아키텍처 (핵심)
- **모든 영역 = 위젯** 모델 확립. **고정 위젯**(상단 메뉴 위젯·사이드 위젯, 편집 제외·sticky) vs **캔버스 위젯**(편집 가능).
- **레이아웃 규칙 확정**: 상단 메뉴 위젯(콘텐츠 폭 라운드 카드·sticky) + 사이드 위젯(라운드 카드·top 정렬) + 캔버스 bento + **도우미 사이드 밑 부착**. 모든 위젯 상단 라인을 첫 컴포넌트와 정렬.
- **편집 모드**(폰 홈 UX): 점선 스냅 그리드 · 삭제 ×(우상단)만 · 8핸들 리사이즈 · 지글 · 드래그 · 추가 팔레트.
- **사이즈 규격**: 4-col 셀(sm 1×1 ~ xl 4×2), 행 내용 기반(auto), 위젯 min-height 156 · **런처 컴팩트(min-height 0)**.
- **사이드바 IA 카테고리화**: 작업 / 산출·현황 / 정책·기준(전용 도구) + ⚙ 팝업. 분류 기준(위젯 vs 전용 도구 vs 팝업) 명문화.
- **플로팅 도우미 = 기능 허브**: 캐릭터(배경 없이) + 작업 상태창(다크 박스) + 채팅(채널톡 스타일), 빠른칩으로 기능 직접 실행. 사이드 위젯 하단 도킹.

### 3. 캐릭터 (다음프렌즈) · 로고
- Figma(다음프렌즈)에서 4종 SVG 추출 → **프로세스 단계로 매핑**(추출=대식·분석=용희·검수=복실·판정=딱지). "에이전트"가 아니라 프로세스.
- **캐릭터 배경 투명화**: export에 박힌 흰색 풀블리드 rect 제거(clip 마스크는 유지). "떠보임" 해소 → **PRINCIPLES §8 에셋 원칙**에 명문화.
- **PersonaCard**: 야구/포켓몬 홀로 트레이딩 카드(포인터 3D 틸트 + CSS 포일, 라이브러리 0).
- **신규 로고**: 귀여운 카툰 **웃는 teal 프리즘 + 빛 분산 4색** 마크 + "Prism" 워드마크(`assets/logo/`). **`LOGO.md` 가이드** 신규.

### 4. 온보딩 · 입체감 · 정합
- **온보딩**: 첫 방문 스텝바이스텝 코치마크(스포트라이트) + 캐릭터 가이드 → 초기 설정(키 입력)까지, 다시 보기.
- **위젯 입체감**: 페이퍼-플랫 위에 부드러운 드롭섀도우 + 내부 하이라이트 + hover 리프트(절제).
- 위젯 홈 / 위젯 카탈로그 / 갤러리 디자인 통일(아이콘 칩·개성 컬러·기능/정보 칩).

### 5. 가이드 문서 체계 (7종 + 적용 프롬프트)
- **SERVICE_DESIGN.md**(최상위·위젯 홈 중심) · **WIDGET_SAAS_STANDARD.md**(위젯형 SaaS **표준**, 제품 비종속) · PRINCIPLES · COMPONENTS · COMPONENT_ROADMAP · **LOGO** · README.
- **적용 프롬프트** `~/Desktop/PRISM_디자인_적용_프롬프트.md` + 표준 사본 `~/Desktop/위젯형_SaaS_표준_디자인가이드.md`.

## 피드백 반영 (주요)
- 상단 라이닝(촌스러움) 제거 → 개성은 아이콘 칩 컬러만.
- 캐릭터 배경 무조건 투명(원칙화). 캐릭터 크게.
- 위젯 내부 스크롤 지양 → 영역 성장.
- 삭제 버튼 −(마이너스) → **×**.
- 사이드바 = 고정 위젯(라운드 카드), 카테고리 구분선·위계.
- 도우미 = 우하단 플로팅 → **사이드 위젯 바로 밑 부착**(가상 영역 비표시).
- 런처 = 큰 타일 → **컴팩트 바로가기**.
- 상단바 = 스크롤해도 sticky.

## 버그·원인
- **상단 메뉴 위젯 정렬이 계속 깨지던 원인** = `.homehead` CSS 규칙이 **중복**이라 원본(`padding: 8px 2px 24px`, 비대칭)이 수정을 덮어씀 → 중복 제거로 해결.
- 런처 "새 배치 추출**이미지...**" 붙던 버그 = title/sub가 inline → `.ds-launcher__body` flex-column으로 분리.

## 결정·메모
- 위젯형 SaaS 표준을 **제품 비종속 문서로 분리**(`WIDGET_SAAS_STANDARD.md`) — 다른 제품엔 토큰·카테고리·캐릭터만 교체.
- 초기 탐색본(dashboard.html·agent-dashboard.html)은 **위젯 홈에 흡수**(참고용).
- 설정은 **위젯이 아니라 ⚙ 팝업**(전역 구성). 전용 도구(사전·프롬프트·인입)는 사이드바 풀페이지.
- **다음**: serve.py 실제 이행(위젯 홈 기준 UI 교체, 백엔드 불변) · ThumbnailGallery 등 P1 잔여 위젯 · 표준 영어판(요청 시).

— 퇴근. 🛠️
