# Upstage 디자인 적용 (Prism UI)

Prism 로컬 UI는 **Upstage** 디자인 시스템을 따른다. 레퍼런스는 Upstage Console 다크 서피스(좌측 사이드바 + 상단 Solar 녹황 프로모 배너 + 바이올렛 액센트).

## 토큰 (적용값)

| 역할 | 값 |
|---|---|
| Primary (인터랙션 단일 컬러) | `#5b52ff` (hover `#4a42e0` / pressed `#1e116e`) |
| Solar 액센트 (단일 사용 · 배너) | `#d2ff95` (위 텍스트 `#0a0d14`) |
| 다크 캔버스 | `#0b0a0f` |
| 사이드바 | `#000000` ~ `#0b0a0f` |
| 카드 surface | `#141318` / hover `#1a1922` |
| 보더 (헤어라인) | `rgba(255,255,255,0.08)` |
| 헤딩 (다크 위) | `#ffffff` |
| 본문 (다크 위) | `#9aa0aa` |
| 뮤트 라벨 | `#6e7191` |
| 폰트 | Geist (sans) / Geist Mono (code) — 400/500/600 |
| Radius | 이진: `0px` 레이아웃 · `8px` 누를 수 있는 요소 |
| Elevation | 그림자 없음 — surface 대비 + 헤어라인으로 깊이 |

## Do
- 바이올렛 `#5b52ff`를 **유일한 인터랙션 컬러**로 (버튼·링크·활성 표시)
- Solar 녹황 `#d2ff95`는 **상단 배너 1곳에만** (Solar 내러티브 인접)
- 깊이는 **surface 대비 + `rgba(255,255,255,.08)` 헤어라인**으로
- radius는 **0/8px 이진**만
- 본문은 Geist `#9aa0aa`, 헤딩은 흰색

## Don't
- 녹황을 기본 버튼/배경에 넓게 쓰지 않기 (단일 액센트 유지)
- 4·12·16px 중간 radius 금지 (레이아웃 vs 누름 구분 붕괴)
- drop-shadow 카드 크롬 금지 (Upstage는 그림자 0)
- 코발트 블루 기본값 금지 (바이올렛-플럼 축 유지)
- **이모지 금지** → 인라인 SVG 아이콘(`currentColor`)만
- 네 번째 폰트 패밀리 추가 금지 (Geist + mono)

> 브랜드 자산·Espeak 서체는 Upstage 소유 — 레퍼런스 전용. 본 UI는 토큰(컬러/타입/지오메트리)만 분석적으로 적용한다.
