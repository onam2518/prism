# Prism 처음 사용자 가이드

Prism을 처음 여는 분을 위한 안내서입니다. 개발 지식이 없어도 따라올 수 있게 썼습니다.
15분이면 검수 한 건을 끝내고 내 캐릭터가 자라는 것까지 볼 수 있습니다.

---

## 1. Prism이 뭐예요?

한 줄로 말하면 **"AI가 만든 콘텐츠 분석 초안을 사람이 검사하는 도구"** 입니다.

AI(모델)가 콘텐츠마다 리드문(요약 문장), 엔티티(핵심 명사), 인텐트(콘텐츠 성격), 카테고리(주제 분류),
유통 가능 여부(등급) 초안을 만들어 둡니다. 여러분이 할 일은 그 초안이 맞는지 보는 것입니다.

- 맞으면 **정확**, 틀리면 **수정**을 누르고 고칩니다.
- 여러 명의 의견이 모여 합의되면 그 결과가 **정답셋**으로 저장됩니다.
- 정답셋이 쌓이면 관리자는 "어느 모델이 더 잘하나", "이번 프롬프트 수정이 효과 있었나"를 숫자로 확인하고,
  나중에 전용 AI를 학습시킬 데이터와 계획서까지 뽑아냅니다.

즉, **여러분의 검수 하나하나가 팀의 데이터 자산**이 됩니다. 그래서 검수할 때마다 점수·레벨·배지가 쌓입니다.

## 2. 시작하기

### 접속

- **브라우저**: 관리자가 알려준 주소로 접속하거나, 로컬에서 `python3 -m prism.serve` 실행 후 `http://localhost:8765`를 엽니다.

### 로그인

팀 모드라면 첫 화면에서 로그인합니다.

1. 처음이면 **가입** 탭에서 이메일·비밀번호·닉네임·캐릭터를 정하고, 관리자에게 받은 **팀 초대 코드**를 넣습니다.
2. 다음부터는 **로그인** 탭만 쓰면 됩니다. "아이디·비밀번호 저장"을 체크하면 이 기기에서는 자동으로 채워집니다.
3. 로그인하면 어느 기기에서든 내 점수·배지가 이어집니다.

로컬 단독 모드라면 닉네임과 캐릭터만 정하면 바로 시작합니다.

## 3. 화면 한눈에 보기

왼쪽 메뉴 기준으로:

| 메뉴 | 누가 | 무엇을 |
|---|---|---|
| 홈 | 모두 | 오늘의 진척율, 내 캐릭터(레벨·미션·배지), 주간 리그 |
| 콘텐츠 검수 | 모두 | 검수할 콘텐츠 목록과 판정·교정, 모델별 결과 비교 |
| 평가 | 모두 | 정답셋 대비 일치율, 틀린 건 판정, 모델 A/B 비교 |
| 게시판 | 모두 | 기능개선 제안·오류 제보(우리 팀에만 공개) |
| 콘텐츠 관리 | 관리자 | 콘텐츠 넣기(STEP 1) → 모델 돌리기(STEP 2) → 진행 확인(STEP 3) |
| 정답셋 관리 | 관리자 | 정답셋 현황·목록, 학습 반영, 학습 데이터 내보내기 |
| 운영 관리 | 관리자 | 팀 관리(멤버·권한)와 검수운영(여력·일정·일 나누기) 2탭 |
| 사전 · 정책 | 관리자 | 분류 사전(인텐트·카테고리·엔티티)과 판정 정책 관리 |
| 스튜디오 | 관리자 | AI에게 주는 지시문 확인·조정(프롬프트), 토픽 설계 |
| 실험실 | 관리자 | 아직 정식 도입 전인 탐구 기능(사용자·MCP 키) |
| 시스템 설정 | 운영 관리자 | 데이터 삭제, API 키 |

낯선 용어가 보이면 **마우스를 올려 보세요**. 태그와 지표 대부분에 설명 풍선이 뜹니다.

## 4. 검수하기 (팀원의 기본 업무)

1. 왼쪽 메뉴에서 **콘텐츠 검수**를 엽니다. 검수 대상 콘텐츠가 표로 나옵니다.
   - 각 행에는 AI가 만든 초안(등급·리드문·분류)과 그 초안을 만든 모델·버전이 표시됩니다.
2. 행의 **검수하기**를 누르면 상세가 열립니다. 원문과 초안을 비교해 보세요.
3. 초안이 맞으면 **정확**, 틀리면 **수정**을 누릅니다.
   - 수정을 골랐다면 무엇이 틀렸는지 요소(리드문·엔티티·인텐트·카테고리·등급 등)를 고르고 바르게 고쳐 주세요.
   - 고친 내용은 AI 지시문 개선과 학습 데이터에 그대로 쓰이므로, 한 건을 고쳐도 가치가 큽니다.
4. 완료하면 즉시 점수가 오르고(+pt 알림), 홈의 진척율·미션이 갱신됩니다.

알아두면 좋은 것:

- **정책이 헷갈리면 화면 오른쪽 아래 `?`(정책 도움말)를 누르세요.** 인텐트·카테고리·품질 사유·등급의 판정 기준을 검색해 볼 수 있고, 상세를 열어 둔 상태라면 그 콘텐츠에 붙은 값들이 바로가기로 떠서 한 번에 기준을 확인할 수 있습니다. 상세의 값 태그를 눌러도 해당 기준으로 바로 이동합니다. 창은 드래그해서 원하는 위치에 두면 다음에도 그 자리에 열립니다.
- **재검토 배지**가 붙은 콘텐츠는 팀원끼리 의견이 갈린 건입니다. 우선 봐 주세요.
- 사이드바 캐릭터 카드 아래 **팀 퀘스트 카드(🏁 vN 버전 마감 D-n)**는 다음 학습 반영(모델 버전 시한)까지 남은 날과 팀 완주 진행률입니다. 그때까지 모인 검수 의견이 그 모델의 새 버전 프롬프트에 반영되니, 마감 전 완주가 팀 목표입니다. 마감 임박(D-1)에는 리마인드가 뜨고, 반영이 끝나면 완료 축하와 함께 새 퀘스트를 기다립니다.
- 가끔 섞여 나오는 검증 문항(정답이 알려진 문제)의 정답률이 내 "골드" 지표가 되고, 점수 배율에 반영됩니다.
- 미션(오늘의 검수, 골드 정답, 불일치 재검토, 분류 채우기)을 채우면 보너스 점수를 받습니다.
- 레벨은 검수를 쌓을수록 오릅니다. 꾸준히 하면 만렙(Lv.50)과 배지 22종을 모을 수 있습니다.

## 5. 평가 보기

**평가** 메뉴에서는 지금 AI가 정답셋과 얼마나 일치하는지 봅니다.

1. 상단 **평가 기준**에서 어떤 모델을 잴지, 어떤 콘텐츠(전체/평가용)로 잴지 고릅니다.
2. **평가 실행**을 누르면 일치율 요약과 함께 **틀린 건 목록**이 나옵니다.
3. 틀린 건마다 둘 중 하나로 판정해 주세요.
   - **채택**: AI 결과가 맞고 정답셋이 틀렸어요 (정답 교정 후보로 올라갑니다)
   - **탈락**: 정답셋이 맞고 AI가 틀렸어요 (AI 개선 근거가 됩니다)
4. **모델별 비교**에서는 두 모델(A/B)을 같은 정답셋으로 돌려 나란히 비교합니다.

## 6. 관리자 가이드

### 콘텐츠 넣고 돌리기 (콘텐츠 관리)

- **STEP 1 콘텐츠 추가**: 수동(텍스트·미디어·엑셀) 또는 자동(API 연동)으로 콘텐츠를 넣습니다. 미디어(이미지·영상)는 추출 미리보기를 확인한 뒤 **콘텐츠로 등록**하면 메타까지 저장되어 STEP 2 없이 바로 검수 대상이 됩니다.
  - 엑셀은 **엑셀 템플릿** 버튼으로 양식을 받아 채워 올리면 한 번에 여러 건이 들어갑니다.
  - **추가 용도**를 정하세요. 검수용(기본)은 팀 검수 대상이 되고, 평가용은 검수에서 제외되어 평가 전용으로 보존됩니다.
  - 저장하면 사용 모델로 초안이 자동 생성됩니다.
- **STEP 2 모델 실행**: 기본 사용 모델을 정하거나, 모아둔 콘텐츠 전체를 다른 모델로 일괄 재실행합니다(버전 vN으로 기록).
- **STEP 3 실행 큐**: 진행 중 작업과, 추가된 콘텐츠의 용도를 건별로 전환할 수 있는 목록입니다.

### 정답셋과 학습 (정답셋 관리)

- **현황·학습 반영**: 검수 합의는 매일 새벽 한 번 자동 반영됩니다. 급하면 "지금 실행"을 누르세요.
  반영 시 '정확' 합의가 정답셋으로 승격되고, AI 지시문이 보정되며, 버전이 올라갑니다.
- **정답셋 목록**: 쌓인 정답을 확인·정리합니다. 평가에서 '채택' 합의된 건은 "교정 필요"로 표시됩니다. 엑셀로 내려받을 수 있습니다.
- **학습 데이터**: 축적량을 논문 기준치와 대비해 보여 주고, SFT/선호쌍(DPO)/판단근거 데이터셋과 **파인튜닝 소요서(.md)** 를 내보냅니다.

### 프롬프트 조정 (프롬프트 스튜디오)

- AI가 받는 핵심 규칙(4호출 계약)과 예시는 **읽기 전용**입니다. 기준 문서가 바뀔 때만 갱신됩니다.
- 조정은 **모델별 쿡북 래퍼** 단위로 합니다. 모델 계열(GPT/Gemini/Claude/Solar)마다 지시문을 감싸는 틀을 고칠 수 있습니다.
- **최종 프롬프트 미리보기**에서 "이 모델 × 이 호출"에 실제로 나가는 프롬프트 전문을 확인하세요.

### 시스템 설정 (운영 관리자)

- **데이터 관리**: 피드백·콘텐츠·정답셋 삭제, 팀 삭제. 삭제는 되돌릴 수 없으니 주의하세요.
- **API 키**: 통합 라우터 또는 Upstage Solar 키를 등록하면 팀원은 키 없이 사용합니다.

## 7. 자주 묻는 질문

**Q. 검수 목록에 안 보이는 콘텐츠가 있어요**
평가용으로 지정된 콘텐츠는 검수 목록에서 제외됩니다. 콘텐츠 관리의 "추가된 콘텐츠 · 용도"에서 확인하세요.

**Q. 관리자 메뉴가 안 보여요**
운영 관리자(허용목록 이메일)만 전체 관리자 메뉴가 보입니다. 팀 관리자(생성자·위임)는 팀 관리만 추가됩니다. 관리자에게 권한을 문의하세요.

**Q. 연습해 보고 싶은데 실데이터를 건드리기 싫어요**
관리자에게 로컬 연습 서버를 요청하세요. 별도 저장소(qa.db)에 연습용 데이터 12건을 시드해 실데이터와 완전히 분리된 채 전 기능을 써볼 수 있습니다(QA_CHECKLIST.md 참조).

**Q. 용어가 어려워요**
화면의 태그·숫자에 마우스를 올리면 설명이 뜹니다. 그래도 모호하면 README의 핵심 개념 절을 참고하세요.
