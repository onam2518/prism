# Prism 데일리 로그 — 2026-07-07

운영 전환 완료일. Fly 상시 서버 검증 → 검수 UX 신기능 2종(원문 페이지 패널·슈퍼관리자) 배포 →
온보딩·키 로테이션·가이드 정리까지 HANDOFF "다음 단계" 전부 소진. 멀티 세션 동시작업 규칙 신설(CLAUDE.md).

## 배포·운영

- **Fly 상시 서버 검증 완료**: prism-item.fly.dev · backend supabase · configured true · guideUrls 3종 ·
  포트 8765 · auto_stop off · min_running 1 · PRISM_CONFIG=/data/config.json. 전일(07-06) 이 머신에서 배포된
  상태를 검증하고 이날 2회 재배포(기능 반영).
- **배포 자동화**: `.claude/settings.local.json` 권한 규칙(git push·fly deploy 허용). "배포해줘" =
  push → fly deploy → /config·마크업 검증. 배포는 클린 워크트리 스냅샷에서만(CLAUDE.md 규칙 5).
- **팀원 온보딩·service_role 키 로테이션 완료**(사용자 확인). Confluence 사용자 가이드 STEP 0 를
  맥 앱 권장 → **브라우저 접속(prism-item.fly.dev)** 안내로 교체(v7 · 맥 앱 설치 안내 제거 · QA 빌드는 관리자 요청).

## 기능 릴리스 (이 세션)

- **원문 링크 전 구간 보존 + 검수 상세 원문 페이지 패널** (`d080f7d`)
  - source_url 유실 3곳 수정: rerun_content·run_pipeline·단건 추가(add_only)가 4필드로 재구성하며
    실행 순간 supabase source_url 을 "" 로 덮어쓰던 결함. 해시(서비스+제목+부제+본문)에 URL 불포함 → 정체성 무변.
  - 입력 경로: 수동 추가 '원문 링크(선택)' 필드 + 엑셀/CSV 템플릿 '원문 링크' 컬럼(ingest 별칭 기존 지원 ·
    `원문링크`/`링크`/`url`/`link` 등 자동 매핑).
  - 검수 상세: 좌측 패널 '추출 텍스트 ↔ 원문 페이지(iframe)' 탭 · 원문 선택 시 다이얼로그 확장(940→1280px) ·
    **배율 3단계**(작게50·보통75·크게100 · 기억) · 링크 없는 항목(골드 문항)은 텍스트 고정.
    다음(Daum) 기사 페이지는 iframe 차단 헤더 없음 실측(내장 동작 확인) · 차단 사이트는 안내 문구.
  - **주의(실사용 함정)**: 기존 콘텐츠에 URL 만 추가한 재업로드는 save_dedup 이 무변경 skip 하고,
    실행된 건 재업로드 시 미실행으로 리셋되는 부작용 → 기존 건 백필은 별도 기능 필요(신규 배치는 컬럼 포함이면 끝).
- **네이티브 confirm 10곳 → DS 확인 모달(dsConfirm · Promise)** (`d080f7d` 포함)
  - 발단: CDP 브라우저 자동화에서 confirm() 이 렌더러를 블로킹해 탭이 멈추는 문제.
  - ds-dialog 기반 · 파괴 액션 danger 버튼 · 팀 삭제 2중 확인 유지 · Esc/바깥 클릭 = 취소.
- **슈퍼관리자 권한 · 팀 생성자 전용 부여** (`1f0de7d` + DDL `prism_super_admin`)
  - 권한 3단계: 운영 관리자(허용목록 · 전부) > 슈퍼관리자(생성자 부여 · 운영 작업 메뉴 전체,
    시스템 설정·데이터/팀 삭제·API 키 제외) > 팀 관리자(위임 · '팀 관리'만).
  - **지정/해제는 오직 팀 생성자만**: set_admin·set_super 서버 게이트 + UI isCreator 이중 게이트.
    기존에 위임 관리자가 남을 지정할 수 있던 구멍도 함께 봉쇄. 운영 관리자도 지정 불가.
  - supabase `prism_reviewers.super_admin`(기본 false) · supastore is_team_super/set_member_super ·
    메뉴 게이팅 cond `opsadmin`(운영 OR 슈퍼) 신설 · 회귀 테스트 2종.

## 기능 릴리스 (병행 세션)

- 모델러 핸드오프 번들(.zip) · 노하우 궤적+백로그+manifest 증분 (`6e28eb4`)
- 페르소나 능동 생성 · 프로필+행동 로그가 모이면 자동 생성 (`55d2c52`) + 테스트 격리 수정 (`ec7087e`)
- **CLAUDE.md 신설**(`f0c1452`): 멀티 세션 동시작업 규칙 · main 직커밋 금지(브랜치→PR) ·
  커밋 범위 최소화 · 클린 스냅샷 배포 · 허브 파일(serve/page/app.js) 충돌 주의.
  실제로 이날 두 세션이 같은 트리에서 작업하며 훅 단위 분리 스테이징으로 커밋 분리함.

## 검증

- 테스트 105 통과(+슈퍼관리자 티어·생성자 전용 게이트 회귀 2종). em-dash 카피 규칙 유지.
- 실브라우저 E2E: 격리 mock 서버(PRISM_DB·PRISM_CONFIG 스크래치) 가입→검수 상세→원문 탭→배율→
  confirm 모달까지 CDP 로 확인. 운영 반영은 /config·/template.csv·마크업 grep 으로 검증.

## 미결

- 기존 콘텐츠 URL 백필 기능(재업로드로는 불가 · hash 매칭 URL-only 갱신 라우트 필요 시)
- 운영 사이클 개시: 첫 실배치 추가 → 모델 실행 → 퀘스트 생성(learnNextAt 설정) → 검수
- 큐(실행 작업) 단위 퀘스트 설계안(보류 · 사용자 검토 대기) · DPO 선호쌍 축적 등 기존 트랙
