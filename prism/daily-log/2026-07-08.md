# Prism 데일리 로그 — 2026-07-08

첫 실배치 검수 개시 주간의 안정화 날. 인증(세션)·표시(판정/현황)·배포 체험(버전 배너) 3계열을
근본 수정하고, 실측 기반 성능 3차로 첫 로드 4.7MB → 약 1.6MB · 재방문 54KB 달성.
오후 2부는 회의 백로그 12건을 분류해 즉시 반영 3건 + **개선 배치 A~G 전량 반영**.
이 날 총 PR #39~#59 머지·배포(#39~#45 는 7/7 오후분 · #60~#62 는 7/9 오전 머지).

## 운영 개시와 사고 복구 (7/7 오후 · 전일 로그 마감 이후)

- **CI 자동 배포 신설**(#39 · 병행 세션): main 머지 = 테스트 게이트 통과 시 Fly 자동 배포.
  문서(.md·docs/)만 변경이면 배포 생략. 로컬 수동 배포를 대체(CLAUDE.md 규칙 5의 기계 강제).
- **골든셋 테스트셋 5종 변환**: set30_1·set50_1·set50_2·set100_1·set100_2(DB 내보내기 xlsx)
  → 프리즘 업로드 CSV(서비스명·제목·부제·본문·원문 링크 · 총 330건 · 중복 해시 0 · 데스크탑).
  원본 xlsx 는 셀 좌표 없는 내보내기라 내장 파서가 못 읽음(변환본 사용).
  발견: 공식 템플릿 헤더 "콘텐츠 그룹"이 ingest 별칭에 없어 업로드 시 그룹 값 유실
  (회피 = "서비스명" 헤더 · 별칭 추가는 다음 수정 백로그).
- **gpt-5.4 실행 전량 빈 메타 사고** · set30_1 30건 업로드 후 실행 결과가 전부 빈 초안. 원인 3중(전부 실측):
  1. Timely 라우터가 요청에 max_tokens 기본값을 주입하는데 gpt-5 계열이 거부(HTTP400) →
     max_completion_tokens 전환(#40).
  2. 서버 영속 설정 reasoning_effort=high 에서만 gpt 가 temperature=0 거부 · claude 는
     response_format(json_object)·temperature deprecated 거부 → **파라미터 협상**으로 일반화:
     400 안내문에서 거부 파라미터를 배우고 즉시 재시도, 모델별 프로세스 캐시(_PARAM_ADAPT)로
     공유(#41). 로컬 미재현 이유 = 로컬 reasoning 기본값(운영 머신 내부 실행으로 확정).
  3. 재실행 배치 도중 CI 배포로 서버 재시작 → 14/30에서 중단(실행 큐·이력이 메모리라 흔적도
     소실). 수정 배포 후 claude-opus-4-8 30건 재실행으로 복구 · 팀 검수 개시(13표).
- **판정 표시 정합**(#43): 판정을 정확으로 바꿔도 '수정 필요'로 조회되던 건 저장 문제가 아니라
  배지가 팀 합의를 이진 렌더링(good 외 전부 수정 필요)한 것. 다른 검수자의 표와 동점(split)도
  수정 필요로 보였음 → 3상태(정확/수정/의견 갈림·재검토) + '내 판정(mine)' 병기(/raw 제공).
  덤: 재클릭 취소 비교가 팀 합의 기준이라 남의 표와 같은 값 첫 클릭이 취소('')로 전송되던
  오동작을 내 표 기준으로 교정.
- **표시 정합 일괄**(#44): 정답 현황에 '검수 완료(반영 대기)' 타일+안내 신설(골든은 학습 반영
  시 확정이라 반영 전 전부 0 → "표시 안 됨" 오해 방지) · 판정 직후 목록 즉시 동기화(상세는
  행의 사본이라 새로고침 전까지 미반영이던 버그) · 정확 버튼 강조도 내 표 기준.
- 관리자 메뉴 간헐 소실 1·2단: 판정 재시도 ensureAdmin(#42) · 인증 서버 일시 장애를
  '비관리자(200)'가 아닌 503(재시도)으로 구분(#45).

## 관리자 메뉴 소실 최종 해소 + 배포 체험 (7/8)

- **세션 자동 갱신**(#46) · 3중 원인의 마지막: access token 1시간 만료인데 갱신 수단이 없어
  만료 후 관리자 판정만 조용히 실패(읽기 화면은 정상이라 로그인된 것처럼 보임).
  "로그아웃 후 재로그인하면 생김"이 결정적 단서. refresh token 저장 · 부팅 선갱신 ·
  45분 주기 연장 · /admin 은 만료를 401 로 구분 · 401 시 1회 자동 갱신 후 재시도 ·
  갱신 실패 시 만료 안내+로그인 화면. 배포 후 각자 **1회 재로그인** 필요(이후 자동 연장).
- **새 버전 배포 감지 배너**(#47): 서버 부팅 ID 를 SSE 접속 인사(hello)와 /config 로 노출.
  배포 = SSE 재연결 = 부팅 ID 변화 → 하단 배너 "새 버전이 배포되었습니다" + [새로고침] 단일
  유도(분기 없음). 페이지의 벤더 js/css 는 ?v=부팅ID 캐시버스터라 일반 새로고침만으로 새
  스크립트 확정 로드(강력 새로고침 불필요 · 세션 만료 상태면 자연스럽게 로그인 화면).

## 성능 (실측 기반 3차 · #48~#50)

- 사전 실측: 첫 로드 4.7MB(폰트 3.8MB + 텍스트 0.94MB · 압축 0 · 캐시 헤더 0) ·
  API /config 862ms·/arena 734ms(콜마다 새 TLS 도쿄→서울 + 인증 요청마다 팀 조회 1콜).
- 1차 전송(#48): _send gzip 협상(텍스트 1KB↑) · 벤더 캐시(?v= 불변 1년·폰트 30일·기타 1시간 ·
  HTML 은 no-store 유지로 버전 감지와 정합) · supastore keep-alive(http.client 스레드별 재사용 ·
  라이브 계약 22종 통과) · team_of 60s 캐시(/reviewer 시 무효화).
- 2차 자산(#49): Tailwind Play CDN 런타임(397KB + 접속마다 브라우저 JIT) → 사전 빌드
  tw.css 8.8KB. 설정 scripts/tailwind.config.js(인라인 config 1:1) · 재생성
  scripts/build_tailwind.sh(**유틸 클래스 추가·변경 시 실행 후 tw.css 커밋**) · 캐스케이드
  위치 보존(app.css 뒤) · 라이트/다크 실렌더 검증. GmarketSans woff 3종 → woff2(1.8→1.23MB ·
  구 woff 제거 · DMG 번들 동일 절감).
- 3차 폰트(#50): Pretendard 2단 서브셋 · 서브셋 644KB(공식 KS X 1001 커버리지 3,728자 ·
  정적 subset 배포의 cmap 과 동일 글리프 · pyftsubset · wght 가변 축 유지) = 일반 사용 유일
  전송, 풀 2MB 는 서브셋 밖 희귀 글자 등장 시에만 unicode-range 로 추가 로드(글자 누락 없음).
  실브라우저: 홈 로드 시 서브셋만 요청 확인.
- **결과**: 첫 방문 4.7MB → **약 1.6MB** · 재방문 **54KB**(벤더 전량 캐시) ·
  /config 862→약 220ms · /arena 734→약 140ms · /raw 164KB→54KB.

## 검증 체계

- 테스트 105 → **120**(파라미터 협상 7 · 판정 mine 3 · 인증 갱신/일시 장애 4 · 배포 감지 1 ·
  전송 성능 4 등). 라이브 supabase 계약 22종은 keep-alive 전송 경유로 통과.
- 실브라우저 검증: 판정 배지 · 배포 배너 · 폰트 네트워크(서브셋만 로드) · 라이트/다크 렌더.

## 회의 백로그 정리 + 즉시 반영 3건 (오후 2부 · 로그 #51 마감 이후)

- 회의 항목 12건을 코드 근거로 4분류(즉시 구현 / 정책 확정 / 데이터 대기 / 그룹 액션) 후
  정책 3건 확정: ① 엔티티 = **상한 없이 핵심만**(건당 가변 · 복합 명사 원형 유지)
  ② 채택/탈락 문구 = 목록 배지 + 평가 탭 ③ '옹호/반박' 관점 인텐트 = **옹호·지지 / 반박·비판**
  명명으로 신설(자동차 카테고리는 기존 사전 존재 확인 · 세분 보강 관점만 후속).
- **판정 스테일 실시간 동기화**(#52): 목록 재조회(SSE 포함) 후에도 열린 상세·탐색 목록은
  '사본'이라 예전 판정이 보이던 문제 → 최신 fb 를 hash 기준으로 사본에 주입(_absorbFreshFb ·
  편집 중에는 방해 금지) · 내 판정 직후 모든 목록 사본 동기화(_syncFbByHash).
- **검수 UX 묶음**(#53): 리드문을 우측 평가 패널에서 즉시 확인(좌측이 원문 페이지 탭일 때도
  대조 가능) · '완료 처리' 라벨이 검수 완료로 오독되던 것 → **'교정 저장'** 으로 교체 ·
  판정 용어 정비(채택/탈락 배지 등).
- **프롬프트·사전 묶음**(#54): 엔티티 수량 가변(프롬프트 규칙 + agents [:3] 절단 제거) ·
  복합 명사 원형 유지(실키 검증: 전기차 보조금·탄소 중립·국민연금 개혁 원형 추출 확인) ·
  관점 인텐트 2종 신설 + 값 정의 주입(호버 정의 포함). 회귀: 정책 텍스트 가드
  (test_prompt_policy) + 기존 3개 절단 검증을 4개 통과로 전환.

## 개선 배치 A~G (오후 2부 · "전체적으로 다 개선하자" 승인)

- **A 작업 이력 + 판정 표기 재설계**(#55): `GET /history` — 판정(누가·언제·정확/수정)·교정·
  초안 재실행을 병합한 최신순 타임라인(sqlite float·supabase ISO ts 혼재 흡수) · 상세 우측
  접이식 '작업 이력 보기' · 판정 직후 자동 갱신 · 메모 호버. **내/팀 판정 표기 재설계**
  (실시간 피드백 "내 판정: … (위 표기는 팀 합의)"가 불명확): 팀 표 2개↑면 배지에 주어 명시
  **[팀 판정 · 정확/수정 필요/의견 갈림]** + 내 판정 배지 분리 병기 · 단독 판정은 기존 유지.
  좌측 본문의 리드문 박스 제거(#53 으로 우측 이관 완료된 중복) · ingest **'콘텐츠그룹' 별칭**
  추가(7/7 백로그 해소 · 공식 템플릿 헤더 정합).
- **B 원문 링크 백필**(#56): 링크 없이 적재된 과거분 구제 — 시스템 설정 '원문 링크 백필' 패널.
  매핑 파일(xlsx/csv/tsv/jsonl)의 URL + 해시/제목 컬럼을 별칭 사전으로 자동 인식 ·
  해시 우선, 제목은 정확 일치 1건만(동일 제목 다건 = 건너뛰고 표본 안내) ·
  **source_url 단일 필드만 갱신**(초안·판정·적재 시각·검수 키 불변).
- **C 판정 실행취소 + 팀 의견 카운트 정합**(#57): 운영 재현 "팀 표 3개 · 정확 0 · 수정 필요 1"
  의 근본 원인 = 취소가 verdict='' 를 upsert 해 빈 표가 n 에 계속 집계. → **취소 = 표 행 삭제**
  (취소 사실은 patch_log undo:verdict 로 이력에 '판정 취소' 보존) · 집계에서 good/bad 외 표 제외
  (운영 레거시 빈 행도 표시 정합 · 데이터 삭제 없음) · 문구 교체(제안 반영)
  **"팀 의견 · 정확 x개 · 수정 필요 y개"** · 상세에 '내 판정 취소' 버튼(내 표만 취소 · 남은 표
  없으면 미검수 복귀) · 내 표 변경/취소를 카운트·합의 배지에 즉시 반영(_fbRecount · 교정 저장은
  팀 합의가 아닌 '내 표' 전송으로 split 오저장 방지). **추가 수정 이어쓰기**(후속 요청):
  /raw fb 에 내 메모·선택 요소 동반 → 추가 수정(단축키 S 포함) 열면 직전 교정 프리필
  (선두 요소 태그는 저장 시 재부착이라 벗겨 중복 방지).
- **작업 이력 열람 제한**(#58 · 추가 요청): 운영(supabase)에서 팀 생성자·슈퍼관리자 전용 —
  서버 `GET /history` 403 게이트 + UI 섹션 비노출(일반 팀원에겐 버튼 자체가 없음).
- **D 품질 fail-open 제거**(#59): 품질 콜 실패가 2중 fail-open(finalGrade 기본값 G + 검증기
  '사유 없음 → G' 보정)으로 **유통 가능 둔갑**하던 문제 → **판정 보류**(등급 없음 + review
  yellow · 분해형은 일부 실패도 커버리지 불명이라 보류) · 보류 행은 fail_kind=api 적재로
  다음 배치 자동 재판정 대상 · UI '판정 보류 · 재실행 필요' 배지. 부수 발견·수정:
  verify_item 의 엔티티 3개 절단 잔재가 #54 정책(상한 없음)을 실운영 경로에서 무력화 → 제거.
- **E 배포-배치 충돌 가드 + 실행 이력 영속화**(#60): 어제 사고(배포가 배치를 14/30 에서 끊고
  흔적도 소실) 교훈의 기계 강제 — CI 배포 전 운영 /ingest-status running 확인 · 최대 10분
  (15초 간격) 대기 · 초과 시 배포 중단(Actions 재실행 안내). 실행 큐 스냅샷을 reports 패턴
  (kind=jobs · 전역 · 최근 20건 · 상태 전이 시만 기록)으로 영속 · 부팅 복원 시 끊긴 작업은
  **"서버 재시작(배포)으로 중단됨 · 다시 실행하세요"** 표시(유령 진행률 방지).
- **F 인증 만료 처리 통일**(#61): 401 자동 갱신이 ensureAdmin 한 곳뿐이라 만료 시 판정·교정·
  설정 저장이 조용히 유실 → 공통 래퍼 `_afetch`(401 → refresh 1회 → 같은 요청 재시도 →
  그래도 만료면 안내 + 로그인 모달) · 인증 쓰기 45곳 전환(/feedback·/patch-meta·/admin·
  /config·/golden*·/eval-*·/rerun*·/run*·백필 등). 로그인/갱신 플로우·무인증 경로는 자체 처리 유지.
- **G E2E 스모크**(#62): 실서버 subprocess 부팅(임시 DB·임의 포트·mock LLM) → 페이지·번들
  서빙 → 단건 추출(실파이프라인) → 검수 목록 → 판정 → 작업 이력 → 판정 취소 → 링크 백필
  (multipart) → 실행 큐 상태까지 **전 구간 HTTP 계약**을 stdlib 만으로 자동 검증 ·
  CI(unittest discover) 포함 = 이후 모든 main 머지의 배포 게이트.
- 검증: 테스트 120 → **140**(+ 이력 2 · 백필 4 · 취소·카운트 3 · 품질 보류 4 · 실행 큐 영속 2 ·
  E2E 1 · 프롬프트 정책 등) · PR 별 실브라우저 확인(새 판정 표기 3표 시나리오 · 타임라인 ·
  취소 → 미검수 복귀 · 메모 프리필 · 래퍼 경유 판정 저장 · 백필 curl E2E).

## 미결·백로그

- 운영 사이클 개시 대기: 검수 13표(합의 정확 8) 쌓임 → ⚡ 즉시 반영 → 퀘스트 생성.
- 오퍼스 현행 초안 1건 리드문 공백(실행 중 일시 실패) → 해당 건만 개별 재실행.
  (D 반영으로 이제 이런 건은 '판정 보류 · 재실행 필요'로 표면화 + 다음 배치 자동 재판정.)
- 모델별 검수 판정(hash×model) 스키마 확장: 대공사라 배치 제외 · 설계안만 별도 제출 예정.
- 데이터 대기(사용자 입력 필요): 옹호·지지/반박·비판 채택률 검증(C1 데이터) ·
  샘플링 균형 표본(축구·카페·티스토리 기준) · 인텐트 매핑 오류 사례 목록.
- 규모 조건부: /raw 페이지네이션(수천 건 도달 시) · feedback_map 등 전량 스캔 API 증분화(1만+ 시).
- ~~ingest '콘텐츠그룹' 별칭~~ → #55 로 해소. ~~배포-배치 충돌·실행 큐 영속화~~ → #60 으로 해소.
