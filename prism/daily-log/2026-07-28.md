# Prism 데일리 로그 — 2026-07-28

검수 화면의 규칙 정합을 잡고, 모델 선택 UI를 갈아끼운 날. 오전에 "타인 배정분에도 검수하기
버튼이 보인다"는 지적에서 출발해 **화면과 서버 규칙이 어긋나 있던 것**을 찾아 고쳤고(#331),
검수 목록 기본값을 할 일 목록으로 바꿨다(#332). 그 사이 "모델 큐가 전부 이상하게 나온다"는
신고를 파고들어 **라우터 잔액 소진(HTTP 402)** 을 확인했다. 오후에는 참고 UI를 받아 모델
선택 드롭다운을 9곳 통일했다(#334).

병행 세션: 검수운영 메뉴 신설(#333) · 검수 목록 창/상한 순서 결함 수정(#337).

참고: 07-16~27 데일리로그는 공백(멀티 세션 상시 가동 · PR #150~#330 이력 참조).
07-17 로그는 `origin/docs/log-0717` 에 미머지 상태로 남아 있다.

## 배정 배타 규칙 · 화면이 서버와 어긋나 있었다 (#331)

- **신고**: 생성자로 보면 다른 사람에게 배정된 콘텐츠에도 `검수하기` 버튼이 뜬다.
- **진단**: 서버(`reviewops.apply_feedback` · 07-17 `087c5b2`)는 "지정 검수자가 있으면
  지정된 사람만 판정 · **생성자·관리자도 예외 없음**"을 강제한다. 그런데 07-22 커밋
  `36a9d3c` 이 "생성자가 타인 배정분을 못 본다"는 신고를 고치면서 **열람 예외와 함께
  판정 잠금까지** 풀었다. 결과: 버튼도 판정 버튼도 열려 있는데 누르면 서버가 거절하는
  막다른 길. `20-ingest-policy.html` 주석에는 "생성자·관리자 포함"이라 적혀 있어
  원래 의도도 잠금이었다.
- **수정**: 열람 예외와 판정 규칙을 분리했다.

  | 함수 | 역할 | 관리자 예외 |
  |---|---|---|
  | `assignBlocked`(신규·공용) | 판정 가능 여부 단일 원천 | 없음(서버와 동일) |
  | `assignedToOther` | 목록 숨김 | 있음 → 생성자 전체 열람 유지 |
  | `detailAssignBlocked` | 상세 판정 버튼 잠금 | 공용 규칙에 위임 |

- 버튼을 그냥 없애면 이유를 알 수 없어, 자리에 `🔒 담당자 검수`를 두고 클릭하면 열람은
  되게 했다. 잠금 안내 문구가 "콘텐츠 관리에서"를 가리키던 것도 실제 배정 위치(담당 열의
  `지정`)로 정정.
- 회귀 테스트 4건. 화면 규칙과 서버 게이트가 다시 어긋나면 잡힌다.

## 검수 대상 목록 기본을 '미검수'로 (#332)

- 목록이 '검수 전체' 기본이라 이미 판정한 건이 계속 섞여 남은 일이 안 보였다.
  `rawRev` 기본값을 `todo` 로 바꿔 할 일 목록으로 만들었다.
- 완료분이 그냥 사라지면 혼란스러우니 **`검수 완료 N건 숨김 · 보기`** 힌트를 넣었다
  (누르면 전체로 펼침). 힌트와 목록이 같은 모집단을 쓰도록 `rawScoped`(검수 상태를 뺀
  나머지 필터)를 분리.
- 덤: 자동 다음 이동(`detailNextTodo`)이 타인 배정분(판정 불가)에 멈추던 것도 건너뛰게.

## 모델 큐 전건 이상 출력 = 라우터 잔액 소진 (진단)

- **신고**: "방금 돌린 콘텐츠 모델 큐가 전부 값이 이상하게 출력된다."
- **확인**: 최근 배치 200건이 카테고리·엔티티·요약·등급 **전부 빈 값**. 실패 원장
  (`fail_rollup`)에 07-28 자 `http_402` **800건**(200건 × 콜 4종). 라우터에 최소 호출을
  직접 넣어 현재도 같은 응답임을 확인.

  ```
  HTTP 402  {"code":"insufficient_balance",
             "message":"Insufficient balance for this request."}
  ```

- 운영 텍스트 모델은 **Timely 라우터**(`PRISM_TIMELY_KEY` · `router.stg.timelyai.io`)를 탄다.
  코드 결함이 아니라 계정 잔액 문제이고, 과금은 $0 이라 돈이 나간 건 아니다.
- **다만 실패해도 행은 저장된다**: `runops` 가 산출이 비어도 `store_save` 를 태워
  200건이 YELLOW 로 검수 목록에 올라왔다. 폴백 체인(`fallback_models`)은 같은 라우터
  키를 쓰니 함께 402.
- **복구 걸림돌**: 「메타 생성 실패 콘텐츠」의 건별 재실행 목록은 상한이 100건
  (`dashops._RECENT_FAIL_CAP`)이라 실패 200건 중 절반만 등재됐다. 일괄 재실행 범위도
  '미실행만'(해당 없음) / '전체'(600건 · 과금 큼) 둘뿐이라 **빈 산출만 골라내는 범위가 없다**.

## 모델 선택 드롭다운 · 9곳 통일 (#334)

참고 UI(Timely 고객센터 모델 선택)를 받아 같은 결로 만들었다.

- 모델 고르는 자리가 원본 id(`claude-opus-4-8`)를 그대로 뿌리는 네이티브 `<select>` 라
  무엇을 고르는지도 얼마나 비싼지도 알 수 없었다. **9곳 전부** 같은 드롭다운으로 통일
  (사용 모델 · 검수 대상 모델 필터 · 전용 프롬프트 · 평가 기준 · A/B 비교 2곳 · 사전 ·
  프롬프트 스튜디오 2곳).
- `prism/modelmeta.py`(신규): id → 읽는 이름(`Claude Opus 4.8`) · 제공자 계열 · 비용 등급.
- **등급은 실제 누적 비용 원장(cost_rollup)에서 계산**하고 건당 평균도 같이 보여준다.
  등급을 못 붙인 모델도 숫자는 남아 판단 근거가 된다.
- **함정**: 실데이터로 돌려 보니 Opus 에 고비용이 안 붙었다. 오늘 402로 실패한 600건이
  과금 0으로 평균을 끌어내린 탓.

  | | 실행 | 과금 | 건당 평균 |
  |---|---|---|---|
  | 07-15~22 정상 | 627 | $4.23 | **$0.0067** |
  | 07-28 전건 402 실패 | 600 | $0.00 | $0 |
  | 그냥 합치면 | 1227 | $4.23 | $0.0034 ← 고비용인데 무표기 |

  → 과금 0 인 묶음은 표본에서 제외. 표본 20건 미만도 무표기(평균이 튄다).
- **마크업 중복 방지**: 조각에는 `<x-modelpick …>` 한 줄만 쓰고 `page.py` 합성 중에
  드롭다운 마크업으로 펼친다. 같은 드롭다운을 9벌 복사하면 한 곳만 고쳐지는 사고가 난다.
- **메뉴가 잘리던 문제**: 패널이 `overflow:hidden` 인 데다 `:hover` 에 `transform` 이
  걸려 있어(카드 뜨는 효과) `position:fixed` 로도 패널 안에 갇힌다. `x-teleport` 로
  body 에 빼내고 열 때 화면 좌표를 재서 붙였다. `x-transition` 은 텔레포트된 요소에서
  떠날 때 전환이 끝나지 않아(Alpine 3.14) 빼고 `x-show` 만 쓴다.
- 제공자 배지는 외부 로고 없이 **계열 머리글자 + 계열색**(자체완결 · CSP 안전).
- 라우터 모델 목록이 서버(`KNOWN_ROUTER_MODELS`)와 화면(`modelCatalog`) 두 곳에 있어
  동기화 테스트를 뒀는데, 작성 즉시 실제 누락(`openai/gpt-5-mini`)을 잡았다.

## 솔라 재실행 · 목록 모델 표기 문의

- "솔라로 돌리는데 목록엔 여전히 오퍼스로 뜬다" → 정상 동작. 목록의 모델 표기는 **해당
  콘텐츠가 마지막으로 실행된 모델**이라, 재실행이 그 건에 도달해야 바뀐다. 확인 시점
  기준 솔라 처리분이 계속 늘고 있었고(3 → 4 → 59 → 115) 산출도 정상이었다.
- 즉 402로 비었던 220건을 `solar-pro3-260323` 로 다시 채우는 중. **미실행 200건은 이번
  범위 밖**이라 그대로 남는다.

## 배포 차단 · 대기 상태

- #333·#334 의 Fly Deploy 가 **실패**. 원인은 워크플로의 배치 보호 가드다.

  ```
  ##[error]10분을 기다려도 배치가 끝나지 않아 배포를 중단합니다
          · 배치 종료 후 Actions 탭에서 이 워크플로를 재실행하세요
  ```

- 위 솔라 재실행(`ingesting: true`)이 계속 돌아 10분 대기 후 중단됐다. 운영은 **`v1.271`
  (#332까지)** 이고 **#333·#334·#337 세 건이 배포 대기**다. 배치 종료 후 재실행하면 된다.

## 병행 세션

- **검수운영 메뉴 신설**(#333): 누가·언제·얼마나 검수할 수 있는지가 없어 배정이 감으로
  나가던 문제. 운영 실측(900슬롯 일괄 배정 후 11일간 조정 없음 · 처리 속도 23배 차이 ·
  미완료 293건이 일부 인원에 묶임 · 2명 유휴)을 근거로 여력·일정 관리 화면을 만들었다.
- **검수 목록 창/상한 순서 결함**(#337): `raw_rows` 가 최신 200건을 자른 **뒤에** 미실행·
  홀드아웃을 걸러내 검수 대상 아닌 행이 창을 먹었다. 미실행 콘텐츠가 200건을 넘으면 표가
  통째로 비는 결함. 제외 규칙을 먼저 적용하도록 뒤집고, 완료 필터는 창을 2000건으로 확대.
  #332 의 '완료 기본 숨김'이 이 잠복 결함을 드러낸 셈이다.

## 오후 이후 (#338~#350 · 로그 작성 뒤 이어진 작업)

이 로그는 #338 시점에 썼고, 같은 날 뒤이어 아래가 머지·배포됐다.

- **운영 관리 용어 통일**(#340): '넘길 계획 보기'·'멈춘 일 넘기기'처럼 완곡한 표현이라
  무엇을 하는 기능인지 화면에서 읽히지 않았다. 뜻이 하나로 읽히는 운영 용어로 바꿨다
  (한눈에 보기→현황 · 일 나눠주기→배정 · 멈춘 일 넘기기→정체·부재분 재배정 ·
  참여 중→활동 중 · 어림값→추정값). 상·하위 탭 간격 50px→12px — 원인 하나가 **전 화면
  공통 결함**이었다(콘텐츠 에이전트 토스트 래퍼가 position:fixed 인데 .canvas 의 flex
  아이템으로 남아 화면마다 gap 한 칸을 더 벌리고 있었다 → display:contents).
- **주간 운영 기록**(#340): 현황판은 '지금'만 보여줘 주가 지나면 그 주가 어땠는지가 남지
  않았다. **마감된 주를 한 번 계산해 고정**한다(weekops.capture · 게으른 적립). 지난 주를
  나중에 계산하면 값이 흔들리기 때문 — feedback 은 upsert 라 재검수 시 과거 실적이 줄고,
  활동 원장은 07-22 부터만 있다(실측: 같은 1주차 검수량이 원장 214건 vs 역산 591건).
  1주차 = 07/20~26(월~일).
- **최종검수자 배정 제외**(#341): 기초 판정이 갈렸을 때 확정하는 2층 역할이라 같은
  콘텐츠의 기초 검수를 맡으면 자기 판정을 자기가 확정한다. 자동 3경로(`_assignable`)와
  수동 2라우트(`_drop_finals`) 모두에서 제외 — 화면만 막으면 API 직접 호출로 새기 때문.
  '여력 비례 배정'→**자동 배정** · '직접 지정 배정'→**수동 배정** · 배정 이력이 uuid 를
  그대로 노출하던 것 → 닉네임.
- **주간 본인 확인 팝업**(#342·#343): 주간 가용 시간은 '그 주의 약속'인데 검수운영 탭이
  슈퍼관리자 전용이라 **본인이 확인할 자리가 아예 없었다**(관리자가 대신 눌러주는 표시).
  주차가 넘어간 뒤 첫 로그인에 1회 받고, 확인 전에는 화면을 진행시키지 않는다.
  확인 주차는 서버가 정한다(본문으로 미래 주차를 넣으면 영구 면제가 된다).
  ⚠ #343 은 실사용 신고 수정 — POST 메뉴 권한 게이트가 **접두 매칭**이라 `/crew-confirm`
  이 `/crew`(슈퍼관리자 전용)에 걸려 일반 검수자가 확인 자체를 못 했다.
- **팀에서 제거한 사용자가 계속 노출**(#344): 명단을 '현재 팀원'이 아니라 이력 합집합
  (판정·프로필·배정)으로 만들고 있어, 팀에서 뺀 뒤 **프로필만 남아도** 살아남았다.
  현재 팀원과 교집합으로 제한 · 명단에서 빠지며 미완료 배정이 조용히 사라지는 것도
  `summary.orphan` 으로 드러냈다(그 콘텐츠는 아무도 못 보는 상태다).
- **배정 가중을 '이번 주 신고'쪽으로**(#345): 신고 시간이 전원 기본값 2.0h 라 캐파 차이가
  전적으로 과거 속도에서 나왔다(최대/최소 4.1배 · 상위 3명 40%). `rate_shrink`(0.5)로
  실측 속도를 팀 중앙값 쪽으로 당겨 4.1배 → **2.1배**. `unconfirmed_factor`(0.8)로
  이번 주 미확인자는 보수적으로.
- **배정 하한·상한**(#346): 하한 `min_fill_ratio`(0.5) — 전원이 캐파 절반을 넘기 전에는
  아무도 그 위로 못 간다(소수에게 몰아주고 나머지를 0건으로 두는 것도 쏠림). 상한
  `cap_limit`(1.0) — 잔여가 캐파를 넘긴 사람은 새 배정을 뒤로 미룬다. **성과 판단이 아니라
  용량 판단**이다. 다른 후보가 없으면 받는다(배정이 멈추면 안 된다).
  · 논의 경위: 처음엔 정체자 제외를 제안했다가, "저번주에 못한 게 다른 사정일 수도"라는
    지적에 철회했다. 애쉬는 4주 실적 36건이 있고 상태·부재·메모가 전부 비어 있었다 —
    **사정을 적을 자리가 없었던 것**이 문제였고, 그건 #342 팝업이 메운다.

## 운영 메모

- **워크트리가 사실상 필수가 됐다**: 이날 다른 세션이 `serve.py`·`00-head.html`·
  `14-golden.html`·`app.css` 를 동시에 고치고 있어 #334 는 처음부터 별도 워크트리에서
  작업했다. 공유 트리의 남의 미커밋 변경은 한 번도 건드리지 않았고, 충돌 2건(script 태그 ·
  CSS 블록)은 둘 다 '파일 끝에 각자 덧붙임' 형태라 양쪽을 그대로 살려 해결.
- 세션 시작 시 워크트리 정리: 죽은 워크트리 3개 prune · 머지 없이 닫힌 PR(#77·#78·#82)의
  모바일 브랜치 3개 삭제.
