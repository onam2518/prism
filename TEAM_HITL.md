# Prism 팀 실시간 휴먼인루프(HITL) — 정책 · 운영

Prism은 실체계 투입 프롬프트·산출물을 **평가·튜닝하는 플랫폼**이다. 그 평가 신호의 원천은
사람 검수(휴먼인루프)다. 이 문서는 **로컬이지만 팀이 공유**하는 검수 환경의 정책을 정의한다.

## 1. 배치 — 공유 인스턴스

- 팀원 1대(또는 내부 소형 호스트)에서 serve 를 **LAN 바인드**로 띄우고, 그 SQLite DB 를 공유한다.
  각자 자기 localhost 로 띄우면 DB 가 분리돼 협업이 성립하지 않는다.
  ```bash
  python3 -m prism.serve --host 0.0.0.0 --port 8765   # 팀 접근(신뢰 내부망 가정)
  ```
- 신뢰된 내부망 가정. 외부 노출 시에는 §5 인증을 먼저 적용한다.

## 2. 검수자 식별 — 인증이 아니라 귀속

- **식별(누가 했나) ≠ 인증(본인 증명).** 현재는 **이름 기반 식별**: 첫 방문 시 이름을 입력하면
  `localStorage(prism_reviewer)` 에 저장되고, 모든 피드백·검수에 `reviewer` 로 귀속된다.
- 스키마(`feedback.reviewer`)는 **인증 불문**이다. 후일 인증을 붙이면 인증된 user_id 가
  같은 자리에 들어갈 뿐, 재작업이 없다(§5).

## 3. 다중 의견 · 합의 — 의견을 덮어쓰지 않는다

- `feedback` PK = **(content_hash, reviewer)**. 검수자별 의견을 모두 보존한다(같은 검수자는
  자기 의견을 멱등 갱신). 단일 의견으로 덮어쓰지 않는다.
- 집계: 한 콘텐츠에 대해 `consensus`(good/bad/**split**) · `agree`(만장일치) · `verdicts[]` 를 제공.
  **불일치(split)는 숨기지 않고 표면화**한다(팀 합의 점검·난도 신호).
- 통계: `feedback_stats` 가 `contents`·`reviewers`·`split` 를 보고한다.

## 4. 검수 큐 · 실시간 협업

- **검수 큐**(`/queue`): 자동(임베딩-LLM)이 확신 못 한 **YELLOW** 콘텐츠 대기열. `only_unreviewed`
  로 아무도 검수 안 한 것만 우선 처리.
- **실시간(SSE `/events`)**: 피드백 저장 시 접속 중 전원에게 브로드캐스트 → 다른 검수자의 검수가
  즉시 토스트로 뜨고 큐·집계가 자동 갱신된다.
- **동시검수 표시(`/presence`)**: 같은 항목을 다른 검수자가 보면 "○○ 보는 중" 배지로 중복 검수
  충돌을 사전 경고한다.

## 5. 후속 정책층 (필요 시)

- **경량 인증**(이름+PIN): 임포스터 방지·권한 구분이 필요할 때. 스키마 그대로, `reviewer` 가
  인증된 값이 됨.
- **리드 승인 2단계**: 검수자 의견 → 팀리드 승인/반려. `approval_status` 컬럼 추가로 확장.
- **검수 할당**: YELLOW N건을 특정 검수자에게 배분(`assigned_to`).
- 이 단계는 권한·워크플로 결정이 선행돼야 하므로, 운영 필요가 확인되면 착수한다.

## 6. 평가 플랫폼과의 연결

검수 의견(휴먼인루프)은 두 곳으로 흐른다:
1. **평가 방법론** — 사람 판정 = 정확도·미탐률 측정의 정답 신호(골든셋 보강). 방법론 비교는
   `abtest`(prism ab)로 정량화한다.
2. **프롬프트 엔지니어링** — `bad` + 교정 메모는 **REAP 피드백 하네스**(`feedback_loop.py`)가
   Remember→Explain→Ask→Plan 으로 가공한 **plan**(구조화 개선 지시)으로 `LEARNED` 에 누적되어
   다음 추출 프롬프트에 단계별 자동 반영된다(학습 루프). raw 메모를 그대로 넣지 않는다.
   원천 프롬프트는 **프롬프트 스튜디오**에서 편집.

## 7. REAP 피드백 원칙

사람 피드백은 비난·감정이 아니라 개선으로 흘러야 한다. 검수자가 `문제`+메모를 남기면 REAP
에이전트가 백그라운드로 처리한다(실시간 토스트로 반영 알림):

- **Remember(기억)** — 비난·감정을 걷어내고 객관 사실부터: 에이전트 산출 vs 검수 지적.
- **Explain(설명)** — 잘된 점·부족한 점을 구체 근거(필드·값)로.
- **Ask(질문)** — 검수자가 원한 올바른 결과를 확인하는 질문.
- **Plan(계획)** — 다음 추출에 적용할 개선 지시(프롬프트에 덧붙일 명령형, 일반화).

`plan` 이 `LEARNED` 로 흘러 단계 프롬프트를 보정한다 → 같은 실수의 반복이 줄어든다.
