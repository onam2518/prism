# 실데이터 / 산출물 (공개 제외)
data/*.jsonl
!data/*.template.jsonl
results*.jsonl
*.db
*.db-wal
*.db-shm
.emb_cache.json
.usage.jsonl
runs/
config.json
prompts/quality.json
*.html
!docs/*.html
!prism/ui/*.html
!.claude/skills/**/*.html

# 내부 참조 로그 — 프로젝트 로그는 내부 식별자(DNM·Confluence 등) 포함 가능 → 공개 제외.
daily-log/
# 단, 디자인 전용 로그(design-system/daily-log)는 내부 식별자 없음 → 추적.
!design-system/daily-log/
!design-system/daily-log/**

# 파이썬
__pycache__/
*.pyc
.DS_Store

report_results.jsonl
*.report.jsonl

# macOS 패키징 산출물
dist/
build/
*.dmg
dict_overrides.json
.claude/settings.local.json

# Astryx(디자인 토큰 참조용) · 런타임 의존 아님
node_modules/
!prism/vendor/compare-window.html
